apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-internet-egress
  namespace: connectivity-tests
spec:
  podSelector:
    matchLabels:
      role: internet-deny
  policyTypes:
    - Egress
  egress: [] # Empty egress blocks all outbound traffic