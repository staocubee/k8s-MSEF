apiVersion: v1
kind: Pod
metadata:
  name: isolated-app
  namespace: isolated-target
  labels:
    app: target-service
spec:
  containers:
    - name: web
      image: nginx:alpine
      ports:
        - containerPort: 80