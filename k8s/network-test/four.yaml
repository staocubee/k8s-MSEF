apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-cross-namespace-ingress
  namespace: isolated-target
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    # Restrict ingress solely to pods inside isolated-target
    - from:
        - podSelector: {}