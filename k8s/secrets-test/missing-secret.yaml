apiVersion: v1
kind: Pod
metadata:
  name: missing-secret
  namespace: hardened
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: nonexistent-secret
              key: password