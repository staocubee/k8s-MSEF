apiVersion: v1
kind: Service
metadata:
  name: baseline-nginx-service
  namespace: baseline
  labels:
    app: baseline-nginx
    environment: baseline
spec:
  type: ClusterIP
  selector:
    app: baseline-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP